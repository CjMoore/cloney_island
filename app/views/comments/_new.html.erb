<%= form_for([@current_project, @comment]) do |f| %>

  <%= f.label "Your Comment" %>
  <%= f.text_area :content, class: "materialize-textarea" %>

  <%= f.submit "Submit Comment", class: "btn btn-color" %>

<% end %>
